name: bcv_tracker_app
description: "Mobile application to monitor and manage official exchange rates from the Central Bank of Venezuela (BCV), featuring real-time dashboards, currency conversion, and multi-language support."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.1.0+3

environment:
  sdk: ^3.9.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package utils to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# utils can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which utils have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  dio: ^5.7.0
  equatable: ^2.0.8
  flutter_dotenv: ^5.1.0
  flutter_svg: ^2.0.11
  get: ^4.6.6
  intl: ^0.18.1

  shared_preferences: ^2.2.3
  url_launcher: ^6.3.1
  skeletonizer: ^2.0.0

  firebase_core: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  change_app_package_name: ^1.4.0
  flutter_launcher_icons: ^0.14.3
  alchemist: ^0.14.0
  # Only the tests reach for it: `external_link_test.dart` swaps
  # `UrlLauncherPlatform.instance` for a fake, which is the supported way to
  # assert what the app asked the platform to open without touching a device.
  url_launcher_platform_interface: ^2.3.2

# Los iconos se GENERAN; no se editan a mano. Las dos fuentes de abajo salen a
# su vez de `assets/brand/dt_icon.svg` con `flutter test tool/generate_app_icon.dart`
# — ver `assets/brand/README.md`. Antes esto apuntaba a un PNG de 332x332, que
# es lo que se veía pixelado en densidades altas (#10).
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/brand/app_icon.png"
  # Web apagado a propósito: este proyecto no tiene `web/index.html`, sin el
  # cual la herramienta aborta el paso web — y de paso pisaba `favicon.png`
  # dejándolo en 16x16. Los iconos web los escribe `tool/generate_app_icon.dart`
  # desde el mismo maestro, así que salen del mismo sitio y con el mismo pixel.
  web:
    generate: false
  # Color plano en vez de una imagen: el fondo adaptativo de Android solo tiene
  # que ser el lienzo, y el PNG anterior (1920x1306, ni siquiera cuadrado) se
  # recortaba distinto en cada máscara.
  adaptive_icon_background: "#08253A"
  adaptive_icon_foreground: "assets/brand/app_icon_foreground.png"
  min_sdk_android: 23
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/brand/
    - assets/icons/
    - assets/images/
    - assets/icons/flags/
    - .env

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package utils, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package utils,
  # see https://flutter.dev/to/font-from-package
